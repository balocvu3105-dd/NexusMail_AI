<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- Zero external dependencies: no EF, MediatR, ASP.NET, Logging, DI -->
  </PropertyGroup>

</Project>
